<h1>Listing books</h1>

<table>
  <tr>
    <th>Picture</th>
    <th>Title</th>
    <th>Author</th>
    <th>Published</th>
    <th>Isbn</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td><img width="100" height="100" src="<%= book.picture %>"></td>
    <td><%= book.title %></td>
    <td><%= book.author %></td>
    <td><%= book.published %></td>
    <td><%= book.isbn %></td>
    <td><%= link_to 'Show', book %></td>
    <td><%= link_to 'Edit', edit_book_path(book) %></td>
    <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Book', new_book_path %>
